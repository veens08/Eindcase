import { BezoekFilterPipe } from './bezoek-filter.pipe';

describe('BezoekFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BezoekFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
